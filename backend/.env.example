# Example configuration for backend (never commit real secrets)
DB_HOST=mysql-dev          # hostname inside Docker network (use localhost:3309 if running outside)
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=dwp4_app

# JWT options
JWT_ACCESS_SECRET=yourAccessSecret
JWT_REFRESH_SECRET=yourRefreshSecret
ACCESS_EXPIRES=15m
REFRESH_EXPIRES=7d

# Frontend origin for CORS
FRONTEND_URL=http://localhost:5173

# server port (optional)
PORT=3010
